# 미태리 매출 대시보드 (웹 화면)

이 저장소는 **복사본**입니다. 원본은 비공개 저장소 `mitaly-cloud` 의 `web/` 이고,
`tools/publish_web.py` 가 여기로 내보냅니다. 여기서 직접 고치지 마세요.

매출 데이터는 이 저장소에 없습니다. 로그인해야만 보이며(RLS), 가맹점 계정 정보는 클라우드에 올라가지 않습니다.
